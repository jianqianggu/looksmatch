import React from 'react';
import ReactDOM from 'react-dom/client';
import LooksmatchApp from './components/LooksmatchApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LooksmatchApp />
  </React.StrictMode>
);
